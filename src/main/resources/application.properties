# Application Name
spring.application.name=rate-cs-staff

# Server Port (Render will set PORT environment variable)
server.port=${PORT:8080}

# PostgreSQL Database Configuration
# For local dev: uses your Render external DB URL by default (update host if needed)
# For Render: DATABASE_URL env var is converted in RateCsStaffApplication.main()
spring.datasource.url=${JDBC_DATABASE_URL:jdbc:postgresql://dpg-d6al7l0boq4c73dfeong-a.oregon-postgres.render.com:5432/asn2_psql?sslmode=require}
spring.datasource.username=${DB_USERNAME:asn2_psql_user}
spring.datasource.password=${DB_PASSWORD:VoImq4O6u4DqeAHoo8bAdm2ID5LyGjAr}
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA/Hibernate Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# Thymeleaf Configuration
spring.thymeleaf.cache=false
